# AGENTS.md

このリポジトリで作業するエージェント向けの規約である。
手を動かす前に、変更対象がどのドキュメントの管轄かを確認すること。

## ドキュメントの位置付け

| ファイル | 担当 | 書き換える条件 |
|---|---|---|
| `README.md` | 使う人向けの概要・セットアップ・運用 | 使い方や設定項目が変わったとき |
| `DESIGN.md` | **What**。レーンの定義と人間の関与点 | 仕様そのものを変えるとき |
| `ARCHITECTURE.md` | **How**。検知・実行の方式と、その選択理由 | 実装方式を変えたとき |
| `AGENTS.md` | 作業規約 | 守るべき前提が増えたとき |

- `DESIGN.md`, `ARCHITECTURE.md` は**思想**であり、実装の都合で書き換えない。変更が必要な際には最小限の分かりやすい日本語で内容を更新し、承認を求めること。

## 検証コマンド

変更を終える前に必ず次を通すこと。

```sh
go build ./cmd/autopilot
go fmt ./...
go vet ./...
go test -count=1 ./...

nix build .
```

## コードの規約

- コメント・ドキュメントは日本語、**である調**で書く。
- コメントには「何をするか」ではなく **「なぜそうしたか」** を書く。
