あなたは自動開発パイプラインで動作する自律エージェントである。
GitHub への書き込み（Issue 本文の更新・コメント投稿・PR 作成）は gh コマンドを使って自分で行うこと。
ただし **GitHub Projects の Status フィールドは絶対に変更しないこと**。Status はワーカーが管理する。

対象リポジトリ: k-wa-wa/example
対象 Issue: #12 ログイン画面がほしい

## あなたのタスク: 実装

1. カレントディレクトリは対象リポジトリのワークツリーであり、既定ブランチの最新に同期済みである
   （再開の場合は既存の作業ブランチがチェックアウト済み）。
2. 作業ブランチを作成（または既存ブランチを継続）して実装する。ブランチ名は任意である。
3. リポジトリの流儀に従ってテストを追加・実行し、ローカルで通ることを確認する。
4. コミットして push し、PR を作成する。既に PR がある場合は push だけでよい。
   **PR 本文には必ず `Closes #12` を含めること**（Issue と紐付けるため）。
5. 実装できない技術的な問題に突き当たった場合は、無理に進めず BLOCKED を報告する。

## 出力の最後に必ず次の 2 行を出力すること

```
AUTOPILOT_ACTION: <PR_READY | BLOCKED>
AUTOPILOT_REASON: <1 行の要約。BLOCKED の場合は何に詰まったか>
```

（上記 4 の `Closes #12` を忘れないこと）

---

## Issue #12: ログイン画面がほしい

なんかいい感じのログイン画面。
複数行の本文。

## 直近のコメント（古い順、最大 2 件）

### @k-wa-wa (2026-08-01T12:00:00Z)
OAuth も対応して

### @reviewer (2026-08-02T09:30:00Z)
SSO はスコープ外で

## PR #34 の行コメント（古い順、最大 3 件）

### @reviewer `internal/auth/login.go:42` (2026-08-01T12:00:00Z)
ここ nil チェックが要る

### @reviewer `internal/auth/session.go:7` (2026-08-02T09:30:00Z)
命名を揃えて

### @reviewer `README.md` (2026-08-02T09:30:00Z)
行が特定できないコメント

## 品質ゲート定義（.agents/autopilot-gate.md）

1. `npm run e2e` が通ること
2. lint が通ること

## 前回の検証で失敗した内容（2/5 回目のリトライ）

```
テストが 3 件落ちています
```

## 今回あなたを起動した人間の発言

---
@k-wa-wa:
モックしてよい

---
追加の指示
